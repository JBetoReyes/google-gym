# Copy to .env.local and fill in values for local development.
# In production (Netlify) these are set in the UI — never commit a real .env file.

# ── Supabase ───────────────────────────────────────────────────────────────────
# From: Supabase Dashboard → Project Settings → API
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# ── API URL ────────────────────────────────────────────────────────────────────
# Local dev: point directly at the local API (Docker Compose).
# Production: leave UNSET — netlify.toml proxies /api/* to Fly.io automatically.
VITE_API_URL=http://localhost:8000

# ── Google AdSense (optional) ──────────────────────────────────────────────────
# Leave empty to show the ad placeholder instead of real ads.
VITE_ADSENSE_CLIENT=ca-pub-XXXXXXXXXXXXXXXX
VITE_ADSENSE_SLOT=XXXXXXXXXX

# ── Stripe (publishable key — safe to expose) ──────────────────────────────────
# Use pk_test_... locally, pk_live_... in production.
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_...
