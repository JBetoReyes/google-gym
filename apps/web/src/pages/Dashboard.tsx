/**
 * Dashboard page — wraps DashboardView from components.
 * Premium gate: basic stats always visible; ChartSlider only for premium users.
 */
export { default } from '@/components/dashboard/DashboardView';
