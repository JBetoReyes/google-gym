/**
 * HistoryPage — list of past workout sessions.
 *
 * TODO: Migrate full HistoryView JSX from v1 src/App.jsx.
 * Reference: FEATURES.md §9 History View
 */
import React from 'react';

export default function HistoryPage() {
  return (
    <div className="min-h-screen bg-[var(--bg-page)] p-4">
      <p className="text-[var(--text-muted)] text-sm">
        HistoryPage — migration from App.jsx in progress.
      </p>
    </div>
  );
}
